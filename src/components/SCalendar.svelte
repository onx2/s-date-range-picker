<script>
	export let dateFormat;
	export let disabledDates;
	export let endDate;
	export let events;
	export let firstDayOfWeek;
	export let isoWeekNumbers;
	export let locale;
	export let maxDate;
	export let maxSpan;
	export let minDate;
	export let monthDropdown;
	export let monthFormat;
	export let rtl;
	export let startDate;
	export let today;
	export let weekGuides;
	export let weekNumbers;
	export let yearDropdown;
</script>

<div>calendar {startDate} through {endDate}</div>
